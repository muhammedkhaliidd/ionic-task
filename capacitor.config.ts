import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ionic.task.app.id',
  appName: 'product-viewer',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
